export interface MessageType {
    id: string;
    video: string[];
    message: string;
    images: string[];
    replyId: string;
    createdAt: string;
    receiverId: string;
    senderId: string;
    conversationId: string;
    updatedAt: string;
    createdDate: string;
    updatedDate: string;
    isMessage: string | boolean
}